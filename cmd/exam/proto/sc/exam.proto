syntax = "proto3";
package exam;

message ExamRequest {
    string exam_name = 3;
}

message ExamResponse {
    string exam_no = 1;
}
service ExamService {
    rpc Submit (ExamRequest) returns (ExamResponse);
}